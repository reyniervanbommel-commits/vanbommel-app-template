# PO view tabs — bouwplan

## Taken

1. Pure helpers `src/utils/viewTabs.js` + tests.
2. Server: `normalizeViewState` tabs + vendorAccount; supplier visibility filter.
3. Hook `usePurchaseOrderViewTabs` gekoppeld aan saved-view state (draft vs Save).
4. UI: tab bar, create-from-column, new tab, save this-tab vs group, vendor account op save-dialog.
5. Version bump, tests, quality gate.
