# Vite dev-server: zelfde APP_ENV-label als de DEV-container (build gebruikt VITE_APP_ENV).
VITE_APP_ENV=dev

# Optioneel: override API-proxy (standaard volgt Vite automatisch PORT uit .env)
# VITE_API_PROXY_TARGET=http://localhost:3008
