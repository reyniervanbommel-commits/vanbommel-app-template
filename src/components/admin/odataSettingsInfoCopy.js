export const ODATA_SETTINGS_INFO = {
  page: 'This page is how the app reaches Dynamics 365: URL, company, and login. What is fetched (filters, columns, baseline) is set on the Data model tab. Settings are stored in SQL (dbo.app_settings). The client secret is never shown here; leave it empty to keep the current value.',
  connection: 'Base URL is the D365 environment URL from LCS or Help → About. The path is the purchase-order header entity. Lines are loaded with $expand=PurchaseOrderLines; line write-back uses /data/PurchaseOrderLinesV2. Company is the legal entity (dataAreaId). Timeout is the maximum wait per OData request, in milliseconds.',
  authentication: 'The app uses OAuth2 client credentials: Azure AD app registration (Tenant ID, Client ID, secret) must also be linked in D365 under Microsoft Entra ID applications. Scope is the base URL plus /.default. Leave the secret empty to keep the stored value. Set the expiry date when you rotate the secret so admins are warned 30 days ahead.',
  cache: 'Max orders is a safety cap on the filtered D365 fetch (phase 1). Max retained orders is separate: orders that first matched the Data model filter but later fall out of it are fetched again by key and show as changed. Fetch budget and warning levels follow that retained-orders setting (maximum 10,000). The board cache is refreshed every night. Sync filters themselves are on the Data model tab.',
};
